<div class="pb-5">
    <div class="container">
        <div class="row g-0">
            {% for item in mockups %}
            <div class="col-6 col-lg-3">
                <img src="/images/projects/{{ title | slugify }}/{{ item }}" alt="Preview of a screen from {{ title }}." class="img-fluid">
            </div>
            {% endfor %}
        </div>
    </div>
</div>