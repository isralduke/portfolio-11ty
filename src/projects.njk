---
title: 'Projects | '
layout: base
bodyclass: projects
socialImage: https://isralduke.com/images/photos/isral-crop-1024.jpg
projectLeadSentence: isral Duke designs inclusive and easy to use digital projects.
---

<main class="" aria-labelledby="pageName">
  <div class="container">
    <div class="row">
      <div class="col-12">
        <h2 class="h2" id="pageName">Projects</h2>
      </div>
    </div>
  </div>
  <div class="container">
    <div class="row g-5">
      {% for item in collections.interactive %}
      <div class="col-md-6">
        <a href="{{ item.url }}" class="d-flex flex-column">
          <h3 class="h5 order-1" id="{{ item.data.projectName | slugify }}">{{ item.data.projectName }}</h3>
          <img src="{{ item.data.projectImgSrc1}}" class="img-fluid rounded mb-3 order-0" alt="Preview of {{ item.data.projectsName }}.">
          <p class="order-2" aria-hidden="true">{{item.data.projectPlatform}} &emsp;|&emsp; {{item.data.projectType}} &emsp;|&emsp; {{item.data.projectStatus}}</p>
        </a>
      </div>
      {% endfor %}
    </div>
  </div>
</main>
