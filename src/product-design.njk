---
title: Projects
layout: base
excerpt: isral Duke designs inclusive and easy to use digital projects.
---

<main class="" aria-labelledby="pageName">
  <div class="container">
    <div class="row">
      <div class="col-12">
        <h2 class="h2" id="pageName">Projects</h2>
      </div>
    </div>
  </div>
  <div role="list">
    {% for item in collections.productDesign %}
    <div role="listitem" class="py-5">
      <div class="container">
        <div class="card border-0">
          <div class="card-body">
            <div class="row justify-content-center align-items-md-center">
              <h3 class="visually-hidden" id="{{ item.data.title | slugify }}">{{ item.data.title }}</h3>
              <div class="col-12 col-md-8 col-lg-9 order-md-0">
                <a href="{{ item.url }}" class="text-decoration-none">
                  <img src="/images/projects/{{ item.data.title | slugify }}/feature.jpg" class="img-fluid rounded mb-3" alt="Preview of {{ item.data.title }}.">
                </a>
              </div>
              <div class="col-8 col-md-4 col-lg-3">
                <div class="card">
                  <div class="card-body p-0">
                    <p class="h5 my-0 mb-2 px-3 pt-3" id="{{ item.data.title | slugify }}">
                      <a href="{{ item.url }}" class="d-flex stretched-link text-decoration-none position-relative">
                        {{ item.data.title }}
                        <span class="bi bi-arrow-right-short ms-auto" aria-hidden="true"></span>
                      </a>
                    </p>
                    <ul class="list-group list-group-flush mb-0">
                      <li class="py-1 list-group-item d-flex">
                        <span class="w-50 fs-6 text-muted">Platform</span><span class="w-50 fs-6 text-muted">{{ item.data.platform }}</span>
                      </li>
                      <li class="py-1 list-group-item d-flex">
                        <span class="w-50 fs-6 text-muted">Type</span><span class="w-50 fs-6 text-muted">{{ item.data.type }}</span>
                      </li>
                      <li class="py-1 list-group-item d-flex">
                        <span class="w-50 fs-6 text-muted">Status</span><span class="w-50 fs-6 text-muted">{{ item.data.status }}</span>
                      </li>
                    </ul>
                  </div>
                </div>
              </div>
            </div>
          </div>
        </div>
      </div>
    </div>
    {% if not loop.last %}
    <div class="container">
      <hr>
    </div>
    {% endif %}
    {% endfor %}
  </div>
</main>
